package com.mw.booksearch.presentation.util

import com.mw.booksearch.R

/**
 * Contains screen names for navigation, as well as their accompanying title for the top app bar.
 */
enum class BookSearchScreenNames(val title: Int) {
    HomeScreen(R.string.book_search),
    BookInformationScreen(R.string.book_information)
}